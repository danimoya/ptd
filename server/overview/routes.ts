import type { Express } from "express";

/** Bespoke REST routes for the overview surface (most reads/writes go through POST /api/actions/<name>). */
export function registerOverviewRoutes(_app: Express) {}
