import type { Express } from "express";

/** Bespoke REST routes for the track surface (most reads/writes go through POST /api/actions/<name>). */
export function registerTrackRoutes(_app: Express) {}
